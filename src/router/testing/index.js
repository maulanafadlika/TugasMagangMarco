const express = require("express");
const router = express.Router();
// const { testingModule } = require("../../../modules");

// router.post('/api/send/email', testingModule.sendEmail);
// router.post('/api/send/whatsapp', testingModule.sendwhatsapp);

module.exports = router;
